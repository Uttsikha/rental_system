<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rentalsystem">

    <application
        android:name=".Database.config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Edit"
            android:parentActivityName=".Rooms"></activity>
        <activity android:name=".Rent"
            android:parentActivityName=".Rooms"/>
        <activity android:name=".Add"
            android:parentActivityName=".Rooms"/>
        <activity android:name=".Hire"
            android:parentActivityName=".Rooms"/>
        <activity android:name=".Report"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".Services"
            android:parentActivityName=".MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.rentalsystem.MainActivity" />
        </activity>
        <activity
            android:name=".Settings"
            android:label="Settings"
            android:parentActivityName=".MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.rentalsystem.MainActivity" />
        </activity>
        <activity
            android:name=".Rooms"
            android:label="Rooms"
            android:parentActivityName=".MainActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.rentalsystem.MainActivity" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Rental System"
            android:theme="@style/Theme.AppCompat.DayNight">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>